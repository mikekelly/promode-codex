#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"

python3 plugins/promode-codex/scripts/validate-promode-codex.py --mode source
scripts/check-local-validators
