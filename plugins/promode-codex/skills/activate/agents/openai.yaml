interface:
  display_name: "Promode Activation"
  short_description: "Load Promode's main-agent brief for the current Codex session."
  default_prompt: "$promode-codex:activate"
policy:
  allow_implicit_invocation: false
